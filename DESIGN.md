# ペリペリ デザイン方針

若者向けツールアプリとして、透明感のある中立な見た目にするための設計指針。
実装の現状は SPEC.md、機能の方針は PLAN.md、変更箇所は TODO.md を参照。

対象 OS は iOS 16.0 以上。ここに書かれた API はすべて iOS 16 で利用できる。

## 決定した方向性

**ぷっくりしたクレイ質感で、ゲームのような手触りを全画面に通す。**

2026-09-24 に方針を変えた。それまでは「静かなミニマル。器は黙らせ、人格は秩序・動き・言葉に載せる」
を採っていた（下の「参照した実測データ」「現状の診断」はその時点の記録として残す）。
変えた理由と経緯は `docs/superpowers/specs/2026-09-24-clay-game-ui-design.md` にある。要点は次のとおり。

- 器が黙りすぎて道具の楽しさが無かった。触って気持ちよいことが継続の理由になる
- 保存枚数の上限（`RealmManager.photoLimit`）は制約ではなく「埋める対象」として見せる
- 前方針で禁じた影・グラデーションは「参照アプリが使っていない」が理由で、使うと壊れるからではない

前の方針から残すもの:

| 残す | 理由 |
|---|---|
| 藤色をブランドの軸にする | アイコン・ストア画像との連続性 |
| ライト／ダーク両対応 | 端末設定を無視するアプリにしない |
| 4pt 基準の余白、Dynamic Type、SF Symbols | 崩す理由が無い |
| キーボード拡張で Lottie を使わない | メモリ上限。代わりに自前の軽い動きを使う |
| マスコットを UI に出さない | 見本画像とリリースノートに留める |

人格は次の 3 つに載せる。

- **質感** — 膨らんだ面、くぼんだ空きスロット、押すと沈むボタン
- **動き** — 保存した瞬間にマスがぽんと出る、上限まで埋まると波打つ
- **言葉** — マイクロコピー（変更なし）

## 参照した実測データ

配色やレイアウトの数値は、以下から実測したもの。推測ではない。

| 出典 | 取得方法 |
| --- | --- |
| setlog（New Chat Inc.） | ITmedia 掲載の実スクリーンショット（1200x2608px）を px 実測して pt 換算 |
| 推し活・ライブ記録系 iOS アプリ 12 本 | App Store スクリーンショットを Pillow で減色・座標サンプリング |
| カメラ・写真系 11 本 | 同上。ツールバー領域の支配色を実測 |
| setolog.com | Tailwind CSS のカスタムプロパティ生値（別プロダクトだが参考） |

## 現状の診断

現行実装が上記の方向と食い違う点。

### 1. 配色がダーク固定で、ライトモードに移行できない

`ColorMG.swift` は濃紫 `#240440` とネオングリーン `#26F78E` の 2 色を全画面で使う（bgDark 21 箇所 / acGreen 44 箇所）。`overrideUserInterfaceStyle` も `traitCollection` の分岐も存在しない。

コントラスト比を実測すると、これは構造的な行き止まりだと分かる。

| 前景 | 背景 | 比 |
| --- | --- | --- |
| `#26F78E` | `#240440`（現行の組み合わせ） | 12.67 |
| `#26F78E` | 白 | **1.42** |

現行のアクセントは濃紫の上でしか成立しない。**ライトモード対応とアクセント変更は同時にしか実行できない。**

### 2. アイコンが FontAwesome

FontAwesome を 28 箇所で使い、SF Symbols は 0 箇所。SPM 非対応のためソースを手動同梱している。

### 3. FAB が Material Design の作法

右下に 56pt の円形ボタンを `shadowOpacity 0.3` の影付きで浮かせている（`MainTabViewController.layoutFAB()`）。iOS の作法ではなく、かつ後述の「影を使わない」方針にも反する。

### 4. タイポグラフィが固定 pt

`systemFont(ofSize: 40)` などの数値が直接書かれ、Dynamic Type に追従しない。

### 5. キーボードがホストアプリの外観を無視する

`view.backgroundColor = .bgDark()` で常時濃紫。`textDocumentProxy.keyboardAppearance` を見ていない。

## デザイン原則

1. **秩序を崩さない。** グリッドは常に上限ぶんのマスを見せ、枚数で並びを変えない
2. **膨らみは部品が持つ。** 影・ハイライト・グラデーションは `Clay.swift` の部品だけが描く。画面側で `layer.shadow*` を書かない
3. **文字は高コントラスト。** クレイの面は淡いため、面の上には `textPrimary` を置く。白文字を載せない
4. **画像より目立つ UI 要素を置かない。** 面の色はパステルに留め、彩度で主張しない
5. **動きは意味のある瞬間にだけ。** 押した・埋まった・完成した。装飾のための常時アニメーションはしない

## デザイントークン

### カラー

参照した 5 アプリのベース色を実測したところ、驚くほど収束していた。

| アプリ | ベース色 |
| --- | --- |
| setlog | `#F4F5F5` |
| relight | `#F5F5F5` |
| 推しスケ | `#F6F6F6` |
| シークログ | `#F6F6F6` |
| 推して何日？ | `#F1F1F1` |

**純白 `#FFFFFF` を地に使っているアプリは 1 つもない。** 純白はカード・モーダルに留保されている。黄み寄りのグレージュでもなく、ニュートラル〜わずかに青みのオフホワイトである。

これに倣い、ペリペリのトークンを次のとおり定める。

| トークン | ライト | ダーク | 用途 |
| --- | --- | --- | --- |
| `bgBase` | `#F5F4F8` | `#141318` | 画面の地 |
| `bgSurface` | `#FFFFFF` | `#1F1E24` | カード、セル、モーダル |
| `textPrimary` | `#1A1922` | `#F2F1F5` | 本文・見出し |
| `textSecondary` | `#6A6775` | `#9B98A6` | 補足、メタ情報 |
| `accent` | `#7362AE` | `#B9AEE8` | 塗りボタン、選択状態、アイコン |
| `onAccent` | `#FFFFFF` | `#1C1B22` | accent で塗った面の上の文字 |
| `accentSoft` | `#ECE9F7` | `#2A2540` | 選択状態の下地 |

**無彩色は 1 つも使っていない。** 地も文字色も、アクセントの藤色へごくわずかに寄せてある。純粋な中間グレーは「決めなかった色」に見えるが、色相をわずかに持たせると「選んだ色」に見える。差は数値としては小さく（`#F5F5F5` に対して青が 3 だけ多い）、単体で見て気付く量ではない。

コントラスト比の実測値。すべてテストで検証している（`testTextColorsMeetContrastRequirement` ほか）。

| 組み合わせ | 比 | 判定 |
| --- | --- | --- |
| `textPrimary` on `bgBase` | 15.9 | 十分 |
| `textSecondary` on `bgBase` | 5.11 | 基準 4.5 を満たす |
| `onAccent` on `accent` | 5.20 | ボタンの文字が読める |
| `accent` on `bgBase` | 4.19 | UI 部品の基準 3.0 を満たす |
| `bgSurface` on `bgBase`（面の分離） | 1.09 | **意図的に極低** |
| `accentSoft` on `bgBase` | 1.06 | **意図的に極低** |

**この二極構造が「透明感と可読性の両立」の答え。**
setlog はボタン面 `#FBFBFB` を地 `#F4F5F5` の上に置いており、その差はわずか 1.06:1。輪郭線は無く、極薄の影だけで浮かせている。一方で本文は `#191919` で 16.10:1 ある。**淡いのは面であって、文字ではない。**

### テーマカラーは藤色

アイコンのホログラムシールを敷く藤色（`#B4AED6`）から起こしている。**アプリを開いたときの色が、ホーム画面で押したアイコンの色と同じ**であることが、ブランドとして最も効く一手になる。

ただし藤色そのものは明るすぎて白い文字が乗らない（白との比が 1.9:1 しかない）。**色相を保ったまま明度だけ落とし、`onAccent` との比が 4.5:1 を超える位置で止めた**のが `accent` の `#7362AE` である。アイコンの地とアプリのアクセントは、同じ色相の濃淡違いという関係になっている。

適用先は限定する。

- **使ってよい**: 塗りボタン、選択状態、アイコンの着色、外枠（2〜3pt のアウトライン）
- **使ってはいけない**: 本文の色、画面の地、カードの地

広い面を藤色で塗ると、並んでいるユーザーの画像より器のほうが目立つ。選択状態の下地が要る場合は `accent` ではなく `accentSoft` を使う。地との差が 1.06:1 しかないため、選ばれていることは分かるが主張はしない。

将来ユーザーに色を選ばせる場合も、この規則をそのまま適用する。推しスケ・Oshibana・シークログ・Pics はいずれも同じ構造を採っており、色をユーザーに明け渡しても UI の統一感と可読性が壊れない（提供数はシークログ 36 色、Pics 20 色、setolog 9 系統）。**藤色を既定色とし、課金で全色開放**という切り方ができる。

### タイポグラフィ

システムフォント（San Francisco / ヒラギノ）のみ。独自フォントは同梱しない。

例外はアプリ名の 1 箇所だけで、ここは **SF Pro Rounded の Heavy**（`UIFont.brand()`）を使う。丸ゴシックの太字は角の立った本文書体と質感が違うため、同じ画面にあっても名前が名前として立つ。OS に入っている書体なので、同梱もライセンス確認も要らない。

将来ロゴタイプを起こした場合は、ベクタ画像（PDF、`Preserves Vector Data` を有効）に差し替える。そのため実装ではナビゲーションバーの `titleView` にラベルを載せており、**`UILabel` を `UIImageView` に置き換えるだけで移行できる**ようにしてある。`navigationItem.title` に書体を指定する方法を採ると、指定がナビゲーションバー全体に及んでしまい、この置き換えができない。

参照アプリの実測では、**文字サイズのメリハリは意外なほど小さい**。setolog は 14px と 12px の 2 段階で全体の約 9 割を占め、階層はウェイト（500 と 700 の二値）で作っていた。見出しを大きく跳ね上げるのは App Store のポスター側だけである。

| 役割 | スタイル | ウェイト |
| --- | --- | --- |
| 画面タイトル | `.title3` | semibold |
| セクション見出し | `.subheadline` | semibold |
| 本文 | `.body` | regular |
| メタ情報・キャプション | `.footnote` / `.caption1` | regular |
| ボタン | `.body` | semibold |

`.largeTitle` は使わない。太さは regular / semibold / bold の 3 段階まで。light は使わない。

すべて Dynamic Type に追従させる。

```swift
label.font = .preferredFont(forTextStyle: .body)
label.adjustsFontForContentSizeCategory = true
```

### スペーシング

4pt を基本単位とし、使う値を限定する。

| 値 | 用途 |
| --- | --- |
| 2 | グリッドのセル間（後述） |
| 8 | 密接した要素間 |
| 12 | カード内部の余白 |
| 16 | 画面の左右マージン |
| 24 | セクション間 |

### 角丸

`Radius.small = 16`、`Radius.card = 24`。ピルは高さの半分（`applyPillShape`）。
`cornerCurve = .continuous` を必ず付ける（`applyCornerRadius`）。
2 種類とピルだけを使い、`Radius.small / 2` のような派生値を作らない。

### 影とブラー

**影とグラデーションは `ClaySurface` だけが持つ。** 外側の柔らかい影（下方向、面の `shade` 色）、
上端の内側ハイライト、下端の内側の陰の 3 つで膨らみを作る。`.recessed` は逆向きに配置してくぼませる。
画面側やセル側で `layer.shadowColor` 等を直接書かない。

**ブラー（`UIBlurEffect`）は使わない。** 透明感は面の色と余白で作る。

## グリッド

**この文書で最も重要な節。** ペリペリは画像グリッドのアプリであり、ここが世界観そのものになる。

### 実地の証拠

setlog は 2026 年 7 月 17 日の UI 変更でグリッドを崩し、ユーザーから強い反発を受けた（[ITmedia](https://www.itmedia.co.jp/news/article/2607/22/1260722098/)）。

旧 UI の実測値。

| 項目 | 実測 |
| --- | --- |
| セル間ギャップ | **約 2.0pt** |
| 左右マージン | 約 9.2pt |
| セル角丸 | 約 19.7pt |
| 構造 | 全セルが同一幅・同一高さ・左右フラッシュ揃え |

変更後は縦動画対応のためセルの大きさがばらつき、左右にジグザグにずれ、隙間が生じた。ユーザーの言葉は一貫している。

> 「なにこの隙間。全く可愛くない良さが無くなった戻して」
> 「綺麗に並んでるのがいい」
> 「無駄な空白部分が多く、4人のグループなどではとても見にくくダサい」

運営は 2 日で「クラシックレイアウト」を追加し、4 日で旧 UI を既定に戻した。それでも評価は 3.43 まで落ちて回復していない。

**「可愛い」の正体は装飾ではなく、等分割・フラッシュ揃え・極小ギャップという幾何学的な規律だった。**

### ペリペリの規則

1. **列数は固定。行の高さも固定。** 画像のアスペクト比でセルの大きさを変えない。正方形にクロップする（`.scaleAspectFill` + `clipsToBounds`）
2. **セル間のギャップは小さく保つ。** 2〜8pt。余白でリズムを作ろうとしない
3. **左右をフラッシュに揃える。** 千鳥配置、ジグザグ、Pinterest 型のウォーターフォールは採用しない
4. **セルの追加・削除でレイアウトが組み替わらないようにする**

現行実装（`ChildContentViewController.gridMetrics`）は既に固定サイズの 2 列グリッドになっており、この方針と整合している。**壊さないこと。**

### 空きスロットを見せる

setlog で最も愛されていたのは、投稿していないメンバーの枠が残り `💤` が表示される仕様だった。これを消して詰めた瞬間に「変な余白」と言われている。

> 「投稿してない人が暗い画面で💤表示になるやつが無くなって、変な余白だけになるのほんまにやめてほしいです。可愛くない」
> 「撮っていなくても💤が表示されるのが可愛くて好きでした」

**穴は埋めるより、意味を持たせて残すほうが可愛い。**

ペリペリは無料枠が 10 枚（PLAN.md 方針2）なので、この構造をそのまま使える。**常に 10 マスを表示し、埋まっていないマスは空きスロットとして見せる。**

これには実利がある。

- 残り枠が一目で分かる
- 埋める動機が生まれる（動画で 10 枚プレゼントする企画と直結する）
- **キャップが「制限」ではなく「世界観」になる**

setlog のユーザーは制約そのものを愛していた。

> 「毎時間一つしか投稿できない、この制限が可愛くて、次の時間がくるのが楽しみだった」

10 枚キャップも、課金を迫るための壁としてではなく、**厳選して持ち歩く道具という性格**として提示できる。同じ機能でも、見せ方で意味が変わる。

## アイコン

FontAwesome を廃止し SF Symbols へ全面移行する。使用中の 13 種類はすべて対応する記号が存在する（実際にコンパイルして実在を確認済み）。

参照アプリのアイコンは**細線（stroke）中心**で、塗りつぶしを使うのは Simeji と LINE カメラのみだった。ペリペリは細線を採用する。

| 現行（FontAwesome） | 置き換え（SF Symbols） | 使用箇所 |
| --- | --- | --- |
| `bars` | `line.3.horizontal` | サイドメニュー |
| `download` | `arrow.down.circle` | 保存数の表示 |
| `ellipsisH` | `ellipsis` | その他メニュー |
| `font` | `textformat` | キーボードの文字モード |
| `globe` | `globe` | キーボード切り替え |
| `grinTears` | `face.smiling` | 空状態 |
| `home` | `house` | 本体アプリを開く |
| `images` | `photo.on.rectangle` | キーボードの画像モード |
| `plus` | `plus` | 追加 |
| `question` | `questionmark.circle` | ヘルプ |
| `sortAlphaDown` | `textformat.abc` | あいうえお順 |
| `sortAmountDown` | `arrow.up.arrow.down` | 人気順 |
| `times` | `xmark` | 閉じる |

サイズは pt ではなくテキストスタイルで指定し、文字サイズ設定に追従させる。

```swift
let config = UIImage.SymbolConfiguration(textStyle: .body)
button.setImage(UIImage(systemName: "house", withConfiguration: config), for: .normal)
```

移行完了後、`PhotoKeyboardFramework/FontAwesome/` とフォントファイルを削除する。手動同梱の依存が 1 つ消え、キーボード拡張のメモリ使用量も下がる。

## コンポーネント

### ボタン

`UIButton.Configuration` を使う。参照アプリに倣い、**ピルか真円**を既定形状とする。

| 種類 | 設定 | 用途 |
| --- | --- | --- |
| 主要 | `.filled()` + テーマカラー | 画面に 1 つの主操作 |
| 副次 | `.tinted()` | 補助的な操作 |
| 第三 | `.plain()` | キャンセル、テキストリンク |

### 画像セル

- 角丸 16〜20、`cornerCurve = .continuous`
- 背景 `bgSurface`、**枠線なし・影なし**
- 正方形クロップ
- 情報は最小限。タイトル `.footnote` と保存数 `.caption1` の 2 行まで
- タップ時は `alpha` を変えず 0.97 倍の縮小アニメーション

### 空きスロット

- 塗りは `bgSurface` より一段沈めた面
- 中央に細線の SF Symbol（`plus` など）
- タップで追加導線へ

### 空状態

SF Symbol + 1 行の説明 + 主要ボタンの 3 点構成。ただし空きスロットを常時表示するなら、完全な空状態は初回のみになる。

## 言葉遣い

参照アプリのコピーは作法が驚くほど揃っていた。

- setlog「毎時間のちょっとした瞬間を、友だちと一緒に Vlog に残そう」
- relight「もうがんばらない、ライブの記録。」
- Live Rock「記録する。振り返る。また、行きたくなる。」
- LiveNote「行ったライブを、ぜんぶ記録。」

規則。

1. **常体を使う。敬体（です・ます）は使わない**
2. **読点で一拍おいて、句点で締める**。二節構造
3. **感嘆符を使わない**
4. **システム側の文言に絵文字を入れない。** 絵文字は機能要素（`💤` のような状態表示）とユーザー生成文の中にだけ現れる。ユーザーのレビューは絵文字だらけなので、この非対称が効く
5. **ひらがなを選ぶ。** setlog は「友達」ではなく「**友だち**」と書いている
6. かわいさは可愛い言葉ではなく、**当事者の語彙をそのまま使うこと**で出す

## 画面別の適用方針

### 一覧（マイボード）

**再デザインの前にタブを畳むこと。** 現行 8 タブのうち 6 つは公開フィード由来で、PLAN.md 方針3 により停止予定。消える画面を磨くのは無駄になる（TODO.md 変更セット A）。

- 地を `bgBase`、セルを `bgSurface` に
- FAB を廃止し、ナビゲーションバー右上の `+` に移す
- 10 マスの固定グリッドと空きスロットを実装する

### 投稿・追加

街頭撮影に耐える速度が要件（PLAN.md）。デザイン観点では入力項目を減らすことが最優先で、タイトル必須・ジャンル必須は撤廃する。

### 詳細

画像を全面に出し、操作は下部に集約。通報・ブロックは `UIMenu` にまとめる（通報機能を残す場合）。

### オンボーディング

3 画面すべてでネオングリーンの大見出し（32〜40pt 固定）を使っている。色による強調をやめ、`textPrimary` に置き換える。

**より重要なのは順序。** 以前は Top → Usage（キーボード設定案内）→ Welcome で、価値を体験する前にフルアクセスという重い許可を要求していた。現在は起動時に Top だけを出し、Usage は最初の1枚を保存した後に回している（PLAN.md 方針4）。さらに、キーボードの有効化を初めて検知したときに「送り方」（タップ → 長押しでペースト → 送信）を一度だけ出す。設定手順と使い方は別画面にし、どちらも短く保つ。

## キーボード拡張の特則

### 1. ホストアプリの外観に追従する

```swift
let isDark = textDocumentProxy.keyboardAppearance == .dark
```

キーボードの明暗はシステムのダークモードではなく、ホストアプリが指定する `keyboardAppearance` で決まる。セマンティックカラーだけでは不十分。

### 2. 背景は純正キーボードに合わせる

独自の濃紫をやめ、ライトは `#D1D3D9`、ダークは `#2C2C2E` 相当を使う。画像セルは 1 段明るい面にする。

### 3. タップ領域を 44x44pt 以上にする

誤タップの影響が最も大きい面。上部バーのボタンは実寸を確認し、不足していれば余白で広げる。

### 4. 装飾を持ち込まない

Lottie アニメーションはメモリと起動時間を消費する。キーボード側では使わない。

## 実装方法

`PhotoKeyboardFramework` に `DesignSystem.swift` を新設し、本体アプリとキーボード拡張の両方から参照する。

```swift
public enum Spacing {
    public static let grid: CGFloat = 2
    public static let s: CGFloat = 8
    public static let m: CGFloat = 12
    public static let l: CGFloat = 16
    public static let xl: CGFloat = 24
}

public enum Radius {
    public static let card: CGFloat = 16
    public static let small: CGFloat = 12
}

extension UIView {
    /// iOS 実機と同じスムーズな角丸を適用する
    public func applyCornerRadius(_ radius: CGFloat) {
        layer.cornerRadius = radius
        layer.cornerCurve = .continuous
    }
}
```

配色は同ファイル内で `UIColor { traits in ... }` による動的カラーとして定義する。
Asset カタログではなくコードで持つのは、フレームワークにカタログを追加せずに本体アプリとキーボード拡張の
両方から参照でき、値と根拠（コントラスト比）をコメントで同じ場所に残せるため。`ColorMG.swift` は削除済み。

トークンが壊れていないことは `PhotoKeyboardFrameworkTests` で検証する。
本文と地のコントラストが 4.5:1 を下回らないこと、地とカード面の差が 1.5:1 未満に収まっていること、
使用する SF Symbols が実在することの 3 点をライト・ダーク両方で確認している。

## 移行ステップ

`docs/superpowers/specs/2026-09-24-clay-game-ui-design.md` の「実装の順番」に従う。

| 段階 | 内容 | 状態 |
|---|---|---|
| 1 | 本書の改訂 | 済 |
| 2 | 部品層（面色・ClaySurface・ClayButton・Motion・Haptic） | |
| 3 | AuroraButton の置き換え、Radius の引き上げ | |
| 4 | マイボード（上限ぶんのマス・埋まる・進み具合・コンプリート） | |
| 5 | 画面の適用（Top / HowToSend / Usage / Add / PhotoDetail / メニュー / 起動画面 / 案内行） | |
| 6 | キーボード（XIB のコード化・セル・Lottie 削除） | |
| 7 | ストアのスクリーンショット撮り直し | |

## やらないこと

- レベル、報酬、鍵の解除、ストリーク
- 音
- マスコットの UI への登場
- 独自フォント、ブラー
- 紙吹雪や全画面の演出（コンプリートは波打ちとピルの変化まで）
- ナビゲーションバーへの追加導線の移動
- 千鳥配置・ウォーターフォール・可変セル高
